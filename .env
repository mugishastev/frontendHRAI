# API Endpoint Settings
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# App Settings
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: Any sensitive keys needed purely on the frontend (if any) should NOT be prefixed with NEXT_PUBLIC_

# Cloudinary Keys should be in the BACKEND .env only. 
# Deleted from here to prevent leakage.

